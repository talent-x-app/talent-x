# Lot de correctifs 5 — 2026-08-21

Issu des rapports [`2026-08-20-qa03-parcours-athlete.md`](../rapports/2026-08-20-qa03-parcours-athlete.md)
et [`2026-08-21-qa02-parcours-coach-complet.md`](../rapports/2026-08-21-qa02-parcours-coach-complet.md).

**Un CI rouge, deux défauts hauts, deux arbitrages rendus, une moitié de correctif.**
Le texte ci-dessous est le prompt à coller dans la session de développement.

> **Base : `main` à `384009d`.** La session de correction travaille dans le worktree
> `C:\Users\iconde\talentx-wt\qa-correctifs`, hors OneDrive. L'intégration dans `main` se
> fait depuis le répertoire principal.

---

## Prompt

Tu reprends six points de la campagne de qualification. Le premier **rend le CI rouge
depuis deux jours** ; deux détruisent ou bloquent des données ; deux appliquent des
arbitrages déjà rendus par le propriétaire ; le dernier termine un correctif livré à
moitié.

Ils sont dans Linear, labellisés `qa-campagne` : **TLX-254**, **TLX-259**, **TLX-257**,
**TLX-223**, **TLX-244**, **TLX-248**. Lis chaque ticket : cause, preuve et reproduction y
sont établies, plusieurs portent des mesures faites sur appareil ou en base, avec
horodatages. **Ne re-diagnostique pas, vérifie.**

Lis d'abord `CLAUDE.md` et `CURRENT_SPRINT.md`. Règles du dépôt intégralement applicables.
**Interdiction de modifier `design/`.** `docs/` est ouvert **uniquement** pour TLX-248, qui
ne consiste qu'en ça.

Traite-les **dans cet ordre**, chacun sur sa branche et son commit.

### ⚠️ La commande de vérification a changé

`pnpm --filter @talent-x/mobile test` **ne lance pas la couverture**. Le CI lance
`test:cov`, avec un seuil global (`statements 87 · branches 80 · functions 84 · lines 88`).

**Vérifie avec `test:cov`, pas `test`.** La campagne a annoncé « suite verte » deux fois de
suite sur une vérification qui ne regardait pas la couverture, et a fusionné deux lots sur
un `main` déjà rouge sans le voir. Ne refais pas cette erreur : trois des quatre métriques
tiennent à moins d'un point de leur seuil, donc **chaque ajout de code peut la faire
basculer**.

### 1. TLX-254 — le CI est rouge ⚠️ **à faire en premier**

`main` échoue depuis la fusion du lot 3 (run #261 sur `ce067d9`). Aucun test ne casse :
**branches 79,86 % pour un seuil à 80 %, soit huit branches manquantes.**

**Ne baisse pas le seuil.** Ce serait adapter la mesure au résultat, et le filet cesserait
de signaler quoi que ce soit.

Les pires contributeurs sont antérieurs — les six cartes d'effort totalisent plus de 300
branches non couvertes. **Ne les traite pas ici** : c'est une dette à part, elle mérite sa
propre passe.

Couvre les huit branches là où le lot 4 vient d'écrire. `record-candidates-ui.tsx` a son
**chemin d'erreur entièrement inexploré** — le `throw response` et le `onError` ne sont
jamais atteints. Un athlète qui confirme un record en zone blanche passe par là : ce test a
une valeur propre, indépendamment du seuil. Complète ensuite sur
`CoachSessionDetailScreen.tsx` (20 manquantes) et `SessionDetailScreen.tsx` (23).

**Écris des tests qui méritent d'exister**, pas des lignes choisies pour leur poids
statistique.

### 2. TLX-259 — l'édition d'une séance efface la durée de ses bornes ⚠️ **perte de données**

Le coach change un **titre**. L'app efface `durationSeconds` de l'échauffement et du retour
au calme. La séance cesse alors de compter dans le monitoring de charge : ni ACWR, ni
monotonie, ni contrainte. Rien ne le signale.

Le témoin est contrôlé — trois séances créées identiques par script, seule celle éditée a
perdu ses champs. Le ticket porte le avant/après.

Deux défauts distincts, traite les deux :

1. **La perte à l'aller-retour.** `composite-canvas.tsx` traite les bornes comme des barres
   à `name` + `notes`. Les commits font `{ ...warmup, notes }`, qui préserverait les champs
   inconnus — **la perte est donc en amont, à la lecture du document vers le modèle
   éditable, ou à la sérialisation retour.** Instrumente ce trajet : sérialise une séance
   importée **sans la modifier**, compare au document d'origine.
2. **Le constructeur n'émet jamais de durée.** Les séances créées dans l'app décrivent leurs
   bornes en clair (`"notes": "Footing 12′ · … · ~25 min"`), sans `durationSeconds`. Écrit
   pour un humain, illisible par le calcul. **Aucune séance créée depuis l'app ne produit
   de charge aujourd'hui.**

**Le vrai correctif est structurel** : un aller-retour par l'éditeur ne doit jamais perdre
un champ du document. ADR-18 impose une lecture défensive des conteneurs ouverts ; la
contrepartie — une **écriture conservatrice** — n'est appliquée nulle part.

**Le test qui compte** n'est pas « la durée survit » mais « le document est identique après
une édition qui ne le touche pas ». Un test d'aller-retour sur un document riche portant des
champs que l'éditeur n'expose pas les attrapera tous, y compris ceux qu'on n'a pas encore
trouvés. **Signale ce que tu découvres d'autre au passage.**

### 3. TLX-257 — le coach ne peut plus réaffecter une séance ⚠️ **et ça amende ADR-58**

Après une première affectation, l'écran d'assignation rouvre systématiquement sur sa
confirmation. Le coach ne peut plus affecter à personne d'autre, ni à un groupe.

`onDone` fait `router.back()` sans remettre `confirmed` à zéro. La route porte pourtant
`key={id}` depuis TLX-93.

**Ce cas amende ADR-58, et c'est le point le plus important du ticket.** La règle traite le
_changement de ressource_ ; elle ne dit rien de la _ré-entrée sur la même ressource après un
parcours terminé_. C'est la seule des treize routes qui portait déjà sa clé, et elle échoue
quand même. `routes-key.test.ts` ne peut pas l'attraper : la clé est bien là.

**Écris la portée de la règle dans ADR-58** — c'est la seule exception à l'interdiction de
`docs/`, et elle est justifiée par le fait que l'ADR est incomplet, pas faux.

Regarde aussi `selected`, `selectedGroups`, `dueDate`, `repeat`, `repeatUntil` : même
mécanisme. Si les cases restent cochées d'une affectation à l'autre, **une affectation
partirait avec une sélection que le coach n'a pas faite** — silencieux, donc pire que
l'écran bloqué.

**Test attendu** : affecter, « Terminé », revenir sur **la même** séance, retrouver le
formulaire. Un test qui change de `sessionId` passe déjà aujourd'hui et ne prouverait rien.

### 4. TLX-223 — `attemptsPerBar` ignoré par la saisie ✅ **arbitré**

Le propriétaire a tranché : **« la saisie doit le lire »**. Le champ reste sur la carte
coach, c'est la grille athlète qui s'y conforme.

Remplace la constante `ATTEMPTS_PER_BAR = 3` par une lecture du bloc, avec la lecture
**défensive** qu'impose ADR-18 — un bloc sans le champ retombe sur 3 sans casser. C'est le
patron déjà en place pour `bars` et `reps` juste à côté.

**Borne la valeur, et borne-la identiquement des deux côtés.** La constante protégeait
implicitement d'une grille à 50 essais par barre ; deux bornes divergentes recréeraient le
défaut sous une autre forme.

**Attention à la réhydratation** : les perfs déjà saisies l'ont été sur une grille à 3.
Relire une ancienne perf sur un bloc configuré à 2 ne doit pas escamoter le troisième essai
enregistré — la grille s'adapte à la donnée, pas l'inverse.

### 5. TLX-244 — le compteur de marques, la moitié serveur

Le correctif client est **prouvé** sur appareil : « 2 marques sur la période » pour deux
marques le même jour. Mais deux lignes plus bas, la ligne de saison dit « 1 marque » pour
**les mêmes deux marques**.

```json
"points":      [ { "date": "2026-08-20", "value": 6.39, "others": [6.65] } ],
"marksByYear": [ { "year": 2026, "best": 6.39, "count": 1 } ]
```

`marksByYear.count` compte les **points**, pas les marques : `others` est ignoré dans
l'agrégation par année. La contradiction a changé de place au lieu de disparaître.

Corrige le comptage dans la dérivation de progression (ADR-34 / TLX-131). **Test
d'intégration avec deux marques le même jour** — sans ce cas, tout test reste vert à tort,
et c'est exactement ce qui a laissé passer la première moitié.

### 6. TLX-248 — deux ADR se contredisent ✅ **arbitré**, `docs/` uniquement

ADR-36 §3 dit que le coach voit les marques libres sous consentement. ADR-51 §D3 borne
toute lecture coach aux séances dont il est l'auteur, ce qui les exclut. Le code applique
ADR-51 — **vérifié, cinq lectures coach avec témoin positif côté athlète.**

Le propriétaire a tranché : **le coach ne voit pas l'entraînement libre de son athlète,
même consenti.** C'est donc le texte périmé qu'on corrige.

1. Amender **ADR-36 §3** : la vue coach **exclut** les séances libres, renvoi à ADR-51 §D3.
2. Ajouter ADR-36 aux références d'**ADR-51** et à sa ligne du journal, avec la mention
   « amende » déjà en usage.

Aucun code, aucun test. Le texte périmé est le seul des deux qui décrit une fuite : quelqu'un
qui le lit aujourd'hui ouvrirait la vue coach aux séances libres en croyant corriger un bug.

### Ce que tu ne fais pas

**Ne bascule aucun ticket en Done.** La campagne les clôt en rejouant sur appareil, pas au
commit — §8 du plan de qualification.

**Ne fusionne pas dans `main` toi-même.** Livre en branches, la campagne rebase et intègre.

**Ne touche pas aux cartes d'effort** pour la couverture (TLX-254) : dette séparée.

### Pour finir

Compte rendu par ticket : ce qui a changé, **ce que les tests prouvent et ce qu'ils ne
prouvent pas**, ce qui reste ouvert.

Les deux dernières sessions ont donné le bon étalon. Celle du lot 4 a expliqué **pourquoi la
campagne n'avait pas pu reproduire TLX-245** — le bloc se démonte sur une séance non
chargée — et a construit son test pour que le cas existe. Sans cette explication, le rejeu
aurait été vert par accident. Celle du lot 3 a **mesuré au lieu d'appliquer** et démontré
que le point 2 de son ticket était faux. **Fais pareil : si un ticket se trompe, dis-le et
prouve-le.**

Signale ce qui t'a paru douteux même hors périmètre.
