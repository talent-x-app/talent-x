# Rapport de campagne — 2026-08-19 — Rejeu du lot 1

Deuxième session. Le lot 1 a été livré, fusionné dans `main` et déployé sur le staging.
Ce rapport rejoue les scénarios que les correctifs devaient réparer. **Les quatre défauts
sont fermés sur mesure — les deux serveur, le parcours de réinitialisation et la cloche,
ces deux derniers joués sur appareil réel. Le lot 1 est clos ; un nouveau défaut
(TLX-235) a été trouvé en rejouant.**

## Contexte

|                      |                                                                                                        |
| -------------------- | ------------------------------------------------------------------------------------------------------ |
| Commit déployé (API) | `3608921` → image `sha-360892142e…` — **à jour** pour le contexte de build d'`apps/api/Dockerfile`     |
| `main`               | `ba98fc4` (le commit postérieur ne touche que la CI et une fiche QA, hors image)                       |
| Staging              | 3 hôtes servis, certificat unique étendu (`staging.talent-x.app` ajouté au SAN), validité → 2026-11-17 |
| Correctifs déployés  | TLX-232, TLX-231, TLX-233, TLX-234                                                                     |
| Préflight QA-08.1    | ✅ vert (voir §Résultats)                                                                              |

## Résultats par scénario

| Scénario                                    | Verdict        | Preuve                                                                                                                            |
| ------------------------------------------- | -------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| QA-08.1 — image à jour                      | ✅             | `git log 3608921..main -- apps/api packages pnpm-lock.yaml …` → vide                                                              |
| QA-08.1 — **readiness (TLX-232)**           | ✅ **fermé**   | `restart api` provoqué par la campagne, puis **1ᵉʳ appel → 200** `{"database":true,"redis":true}` (503 auparavant)                |
| QA-08.1 — certificat                        | ✅             | lignage unique, SAN à 3 noms, même numéro de série sur les trois hôtes                                                            |
| QA-08.1 — providers worker                  | ✅             | `Push réel actif — APNs:prod FCM:on` · `Email réel actif — Brevo`                                                                 |
| QA-08.1 — clé RS256                         | ✅             | `kid` inchangé, aucun avertissement de clé éphémère                                                                               |
| QA-01.2 — **rate limiting (TLX-233)**       | ✅ **fermé**   | rafale → `401 401 401 401 401 429 429 429`. Coupure au **11ᵉ** appel de la fenêtre — exactement `THROTTLE_STRICT_LIMIT=10`        |
| QA-01.3 — rotation / rejeu                  | ✅             | rejeu → 409 `TOKEN_REUSE_DETECTED` ; le jeton légitime issu de la rotation tombe aussi → **famille révoquée**                     |
| QA-01.6 / 01.7 — logout, logout-all         | ✅             | 204 puis refresh → 409 ; la seconde session tombe                                                                                 |
| QA-01.8 — 2FA                               | ✅             | 501 des deux côtés                                                                                                                |
| QA-01.5 — page de réinitialisation          | ✅             | 200, 1828 o, `<title>Nouveau mot de passe · Talent-X</title>`, 2 champs mot de passe, **0 script tiers**                          |
| QA-01.5 — `Referrer-Policy`                 | ✅             | `no-referrer` servi sur la page — contrainte ADR-57, le jeton transite en query string                                            |
| QA-01.5 — jeton invalide                    | ✅             | `POST /auth/reset-password` → **400 `INVALID_RESET_TOKEN`**, message lisible                                                      |
| QA-01.5 — CORS                              | ✅             | `Access-Control-Allow-Origin: https://staging.talent-x.app` sur l'origine du site                                                 |
| QA-01.5 — **parcours réel de bout en bout** | ✅ **fermé**   | joué sur appareil : jeton créé `10:00:50` → consommé `10:01:59` → sessions révoquées **au même instant** → reconnexion `10:02:34` |
| QA-01.5 — usage unique du jeton             | ✅             | second envoi refusé — « ce lien est invalide, expiré ou déjà utilisé » ; aucune 2ᵉ réinitialisation en base                       |
| QA-05.6(c) — **changement de compte**       | ✅ **fermé**   | même jeton natif, **une seule** ligne, titulaire passé de `+qa-reset` à `+qa-athlete` — l'upsert TX-ARCH-001 §4.6 en réel         |
| QA-05.4 — **cloche (TLX-231)**              | ✅ **fermé**   | 2 push émis (worker muet), bannière affichée, **badge incrémenté sans navigation**                                                |
| QA-05.4 — rafraîchissement de la ressource  | ❌ **TLX-235** | la cloche bouge, la liste des séances non — l'app annonce un événement qu'elle n'affiche pas                                      |

## Deux défauts fermés

**TLX-232** — la sonde de disponibilité ne ment plus au démarrage. J'ai refait la mesure
moi-même plutôt que de reprendre celle du déploiement : c'est un test **à un coup**, et
seul un redémarrage frais le rend valide.

**TLX-233** — le limiteur coupe, et au bon endroit. La précision compte ici : cinq appels
consommés par les scénarios fonctionnels, puis coupure au sixième de la rafale, soit le
**onzième** de la fenêtre. Le seuil configuré est de dix. Ce n'est pas « ça coupe », c'est
« ça coupe exactement là où c'est configuré ».

## Deux corrections de la campagne elle-même

**Ma sonde QA-01 se serait sabotée.** L'ancienne version enchaînait six connexions pour
les scénarios fonctionnels puis quinze échecs pour tester le limiteur — sur un budget
**partagé par IP** de dix par quart d'heure. Elle aurait rendu des 429 sur les scénarios
fonctionnels et je les aurais lus comme des régressions. Réécrite : fonctionnel d'abord,
saturation en dernier, et un 429 hors saturation est signalé **non concluant**, jamais
échec.

**Mon contrôle de fraîcheur d'image produisait un faux positif.** Écrit la veille comme
« tout commit non-Markdown périme l'image », il a signalé `ba98fc4` — qui ne touche que
`ci.yml` et une fiche QA, lesquels n'entrent pas dans l'image. La fiche QA-08.1 teste
désormais le **contexte de build réel** (`apps/api`, `packages`, lockfile, workspace),
seule question qui ait un sens.

## Vérifié vs supposé

**Mesuré** — la fraîcheur de l'image par les chemins du contexte de build ; le premier
`/ready` après un redémarrage que j'ai déclenché ; la rafale de connexions et le rang
exact de la coupure ; la page de réinitialisation servie, son titre, ses deux champs, son
absence de script tiers et son `Referrer-Policy` ; le refus d'un jeton invalide en 400 ;
l'en-tête CORS pour l'origine du site.

**Supposé / déduit** — le **certificat** : je reprends les mesures de la session de
déploiement (lignage unique, série identique, `renew --dry-run` en webroot) sans les
avoir refaites. C'est désormais le seul point non remesuré de ce rapport : TLX-231 est
passé de « supposé » à « observé sur appareil » en cours de session.

## Le parcours de réinitialisation, joué en réel

Passé de « supposé » à « mesuré » en cours de session. Le parcours complet a été déroulé
depuis le téléphone, et la base porte la trace de chaque étape :

| Instant    | Événement                                                            |
| ---------- | -------------------------------------------------------------------- |
| `10:00:50` | jeton créé — demande depuis l'app                                    |
| `10:01:59` | jeton **consommé**, et session du 18/08 **révoquée au même instant** |
| `10:02:34` | nouvelle session : la reconnexion, 35 s plus tard                    |

**Une alerte levée par la mesure.** Signalement en cours de test : « j'arrive à ouvrir
plusieurs fois le lien ». La base a tranché avant toute conclusion — deux jetons, tous
deux consommés au **même** instant, aucun jeton créé depuis, et la session de `10:02:34`
jamais re-révoquée : **aucune seconde réinitialisation n'a eu lieu**. Ce qui se rouvre
est la **page**, pas le jeton — `/reset-password` est du HTML statique servi par Nginx,
qui ne peut pas connaître l'état du jeton au chargement. Le second envoi est bien refusé
(« ce lien est invalide, expiré ou déjà utilisé »), confirmé sur l'appareil.

**Réserve d'expérience, pas défaut** : faire remplir un formulaire avant d'annoncer que le
lien est mort n'est pas idéal. Valider au chargement exigerait un endpoint « ce jeton
est-il valide ? » — un oracle interrogeable en masse. Compromis assumé, tracé ici plutôt
que ticketé.

**Détail confirmé au passage** : un jeton de la veille, **déjà expiré**, porte le même
`used_at`. C'est le comportement documenté dans `auth.service.ts` — consommer un jeton
invalide tous les autres jetons en attente du compte.

## La chaîne push, rejouée sur appareil

**Un blocage mesuré avant de tirer.** Le compte `+qa-athlete` était bien connecté sur le
téléphone, mais la base disait autre chose : **une seule** ligne `device_tokens`, encore
au nom de `+qa-reset`, dernier contact `10:02:34` — la reconnexion du parcours de
réinitialisation. Envoyer à cet instant aurait produit « Notification sans cible » et un
faux négatif imputé à la chaîne push. Le compte n'appartenait par ailleurs à **aucun
groupe** : aucune séance ne pouvait lui être affectée.

Une déconnexion/reconnexion a réglé les deux, et a livré au passage un scénario du plan :

|                | avant           | après           |
| -------------- | --------------- | --------------- |
| titulaire      | `+qa-reset`     | `+qa-athlete`   |
| jeton natif    | `fmCQuxjJS6eJ…` | `fmCQuxjJS6eJ…` |
| lignes en base | 1               | 1               |

Même jeton, même ligne, titulaire changé : ni doublon, ni ligne orpheline pointant
l'ancien compte. **QA-05.6(c) est validé en conditions réelles** — c'est le bug de
production que ferme TLX-226, prouvé sur appareil et non plus en test unitaire.

**Les deux envois.** Séances neuves à chaque fois, volontairement : réaffecter la même
via la même clé d'idempotence n'émettrait rien, et l'échec serait indiscernable d'une
panne de la chaîne push.

| Instant (UTC)  | Événement                               |
| -------------- | --------------------------------------- |
| `12:19:07.757` | `session_assigned` — `dedupe …9b6a140b` |
| `12:20:14.529` | `session_assigned` — `dedupe …ddd83f87` |

Worker **muet** sur toute la fenêtre : tout chemin d'échec de `fcm-client.ts` écrit un
`warn`, donc l'absence de trace vaut succès. Deux clés de déduplication distinctes, deux
notifications qui coexistent. Sur l'appareil : bannière au premier plan, et **badge
incrémenté sans aucune navigation** — TLX-231 est fermé.

La seule ligne « sans cible » de la fenêtre vise le **coach**, piloté par script sans
appareil. Comportement attendu, et c'est précisément ce qui bloque QA-05.3 depuis le
début.

**Un troisième trou de TLX-231, trouvé en rejouant.** Remarque de l'utilisateur pendant
le test : la cloche se met à jour, mais **l'accueil non** — la nouvelle séance n'apparaît
qu'après un rafraîchissement manuel. Confirmé par lecture du code : l'écouteur d'arrivée
n'invalide que `NOTIFICATIONS_QUERY_KEY`, jamais `['assignments']`. Et comme
`staleTime` ne déclenche aucun refetch, la liste reste figée **indéfiniment** tant qu'on
ne quitte pas l'écran. **TLX-235** ouvert : l'app annonce un événement qu'elle n'affiche
pas.

## Écarts du registre touchés

| Ligne du registre                               | État                                                                            |
| ----------------------------------------------- | ------------------------------------------------------------------------------- |
| TLX-232 — `/ready` ment au démarrage            | **Résolu** — vérifié sur démarrage frais                                        |
| TLX-233 — aucun rate limiting                   | **Résolu** — coupure au seuil configuré                                         |
| Lien de reset pointant sur l'hôte API (TLX-234) | **Résolu** — parcours complet joué sur appareil, usage unique du jeton confirmé |
| TLX-231 — cloche figée                          | **Résolu** — badge incrémenté sans navigation, sur appareil                     |
| — (nouveau) TLX-235 — ressource non rafraîchie  | **Ouvert** — la cloche bouge, la liste des séances reste figée                  |

## Suites à donner

- [x] ~~**QA-01.5 sur appareil**~~ — joué et validé ; **TLX-234 et TLX-104 fermés**
- [x] ~~**QA-05.4 sur appareil**~~ — joué et validé ; **TLX-231 fermé**, **TLX-235**
      ouvert dans la foulée. **Le lot 1 est intégralement clos.**
- [ ] Ajouter au runbook la normalisation des droits après `scp -r` (piège relevé par la
      session de déploiement : répertoires créés en mode 500, Nginx sert des 404 sur un
      montage pourtant plein).
- [ ] Le §8 du runbook ne vaut que pour une **première** installation (`mv /tmp/staging
/opt/talentx/` donnerait `staging/staging` sur un existant) — lui donner sa variante
      « déjà en service », comme le §9 en a une.
- [ ] Poursuivre la campagne : QA-02 (parcours coach sur appareil, dont `group_update`).
