import { JoinGroupScreen } from '../../../src/groups/JoinGroupScreen';

/** « Rejoindre un groupe » via code (TLX-88) — route empilée hors tab bar. */
export default function JoinGroupRoute() {
  return <JoinGroupScreen />;
}
