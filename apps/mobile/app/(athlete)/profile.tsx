import { ProfileScreen } from '../../src/profile/ProfileScreen';

// Profil athlète (A-10 — TLX-042). Écran partagé piloté par /users/me.
export default function AthleteProfileScreen() {
  return <ProfileScreen />;
}
