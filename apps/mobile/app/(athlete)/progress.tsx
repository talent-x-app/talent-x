import { ProgressScreen } from '../../src/athlete/ProgressScreen';

/** Onglet Progression athlète : graphes par épreuve (A-06) + records (A-07). */
export default function AthleteProgressRoute() {
  return <ProgressScreen />;
}
