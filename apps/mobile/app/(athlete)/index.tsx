import { AthleteHomeScreen } from '../../src/athlete/AthleteHomeScreen';

/** Onglet Accueil athlète → tableau de bord d'accueil (A-01, TLX-089). */
export default function AthleteHomeRoute() {
  return <AthleteHomeScreen />;
}
