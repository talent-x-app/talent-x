import { AthleteCompetitionsScreen } from '../../src/athlete/AthleteCompetitionsScreen';

/** Compétitions de l'athlète (TLX-101, ADR-24) — route empilée hors tab bar. */
export default function AthleteCompetitionsRoute() {
  return <AthleteCompetitionsScreen />;
}
