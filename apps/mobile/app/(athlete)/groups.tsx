import { AthleteGroupsScreen } from '../../src/groups/AthleteGroupsScreen';

/** Onglet Groupe athlète (ADR-44 §3) — accès direct au(x) groupe(s) de l'athlète. */
export default function AthleteGroupsRoute() {
  return <AthleteGroupsScreen />;
}
