import { CoachReviewScreen } from '../../../src/coach/CoachReviewScreen';

/** Revue de performance + feedback (C-08, TLX-086) — route empilée hors tab bar. */
export default function CoachReviewRoute() {
  return <CoachReviewScreen />;
}
