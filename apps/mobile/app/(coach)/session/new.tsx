import { SessionBuilderScreen } from '../../../src/coach/SessionBuilderScreen';

/** Création d'une séance (C-05, TLX-052) — route empilée hors tab bar. */
export default function NewSessionRoute() {
  return <SessionBuilderScreen />;
}
