import { CompetitionBuilderScreen } from '../../../src/coach/CompetitionBuilderScreen';

/** Création d'une compétition (TLX-101, ADR-24) — route empilée hors tab bar. */
export default function NewCompetitionRoute() {
  return <CompetitionBuilderScreen />;
}
