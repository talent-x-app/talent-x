import { CoachGroupsScreen } from '../../src/groups/CoachGroupsScreen';

/** Liste « Mes groupes » coach (TLX-87) — route empilée hors tab bar. */
export default function CoachGroupsRoute() {
  return <CoachGroupsScreen />;
}
