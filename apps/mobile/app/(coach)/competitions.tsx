import { CoachCompetitionsScreen } from '../../src/coach/CoachCompetitionsScreen';

/** Liste des compétitions du coach (TLX-101, ADR-24) — route empilée hors tab bar. */
export default function CoachCompetitionsRoute() {
  return <CoachCompetitionsScreen />;
}
