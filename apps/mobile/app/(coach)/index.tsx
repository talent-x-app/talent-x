import { CoachDashboardScreen } from '../../src/dashboard/CoachDashboardScreen';

/** Onglet Accueil coach → Tableau de bord (C-01, TLX-081). */
export default function CoachHomeScreen() {
  return <CoachDashboardScreen />;
}
