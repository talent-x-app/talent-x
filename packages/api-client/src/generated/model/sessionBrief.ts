/**
 * Generated by orval v8.15.0 🍺
 * Do not edit manually.
 * Talent-X API
 * Contrat d'API REST de Talent-X — source de vérité dérivée de TX-SPEC-002 v2.0 (catalogue d'endpoints, matrice d'autorisation, conventions et format d'erreur) et alignée sur le modèle de données TX-DATA-006.
 *
 * Conventions transverses : préfixe /api/v1 ; jeton d'accès JWT (RS256) via en-tête Authorization ; pagination par enveloppe { data, meta } ; idempotence des écritures sensibles via Idempotency-Key ; opérations longues asynchrones (202 + ressource de statut) ; rate limiting signalé par les en-têtes RateLimit-*. L'autorisation combine rôle, appartenance (lien coach↔athlète), propriété et consentement ; voir TX-SPEC-002 §6.
 * OpenAPI spec version: 1.0.0
 */
import type { CoachNotes } from './coachNotes';

/**
 * Couche éditoriale d'une séance — double lecture coach/athlète (ADR-28). Toutes les propriétés sont optionnelles (brief absent, partiel ou complet). `intent` et `coachNotes` sont retirés de la réponse pour un lecteur athlète (filtrage serveur, jamais côté client).
 */
export interface SessionBrief {
  /** Version du contrat JSONB (cf. TX-DATA-006 §9.x). */
  schemaVersion?: number;
  /** Consigne pour l'athlète, en une phrase (visible coach ET athlète). */
  athleteIntent?: string;
  /**
     * Durée estimée de la séance, en minutes (visible coach ET athlète).
     * @minimum 0
     */
  durationMinutes?: number;
  /**
     * Difficulté cible, 1..10 (visible coach ET athlète).
     * @minimum 1
     * @maximum 10
     */
  difficulty?: number;
  /** Critère de réussite « Réussi si… » (visible coach ET athlète). */
  successCriteria?: string;
  /** Règle d'arrêt « Stop si… », dite à l'athlète (visible coach ET athlète). */
  stopCriteria?: string;
  /** Intention d'entraînement du jour — COACH UNIQUEMENT : absent de la réponse pour un lecteur athlète. */
  intent?: string;
  coachNotes?: CoachNotes;
}
