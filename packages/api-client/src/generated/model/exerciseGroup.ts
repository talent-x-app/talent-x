/**
 * Generated by orval v8.15.0 🍺
 * Do not edit manually.
 * Talent-X API
 * Contrat d'API REST de Talent-X — source de vérité dérivée de TX-SPEC-002 v2.0 (catalogue d'endpoints, matrice d'autorisation, conventions et format d'erreur) et alignée sur le modèle de données TX-DATA-006.
 *
 * Conventions transverses : préfixe /api/v1 ; jeton d'accès JWT (RS256) via en-tête Authorization ; pagination par enveloppe { data, meta } ; idempotence des écritures sensibles via Idempotency-Key ; opérations longues asynchrones (202 + ressource de statut) ; rate limiting signalé par les en-têtes RateLimit-*. L'autorisation combine rôle, appartenance (lien coach↔athlète), propriété et consentement ; voir TX-SPEC-002 §6.
 * OpenAPI spec version: 1.0.0
 */
import type { Exercise } from './exercise';
import type { ExerciseGroupGroupType } from './exerciseGroupGroupType';
import type { ExerciseGroupKind } from './exerciseGroupKind';

/**
 * Groupe d'exercices répété en tours/séries (ADR-27, exercises v3) — séries de courses, contraste/superset, circuits, gammes. **Un seul niveau** : `items` ne contient que des `Exercise`, jamais un groupe.
 */
export interface ExerciseGroup {
  /** Discriminant structurel — présent uniquement sur un groupe. */
  kind: ExerciseGroupKind;
  name: string;
  order: number;
  /** Sémantique d'affichage/guidage (défaut circuit) — pas de mécanique. */
  groupType?: ExerciseGroupGroupType;
  /**
     * Nombre de tours/séries (entier ≥ 1).
     * @minimum 1
     */
  rounds: number;
  /**
     * r — récup entre exercices d'un même tour (s).
     * @minimum 0
     */
  restBetweenItemsSeconds?: number;
  /**
     * R — récup entre tours (s).
     * @minimum 0
     */
  restBetweenRoundsSeconds?: number;
  notes?: string;
  items: Exercise[];
}
