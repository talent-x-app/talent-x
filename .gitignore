# deps & builds
node_modules/
dist/
build/
coverage/
*.log

# env & secrets
.env
.env.*
!.env.example
*.pem
# Credentials des fournisseurs push (TLX-226) : identifiants du projet Firebase et clés
# d'API client. Fournis par la console, jamais versionnés — même règle que le .p8 APNs.
google-services.json
GoogleService-Info.plist

# Expo / RN
.expo/
.expo-shared/
*.jks
*.keystore
ios/Pods/
android/.gradle/
android/app/build/

# Docker — override local machine-spécifique
docker-compose.override.yml

# Claude — config locale de serveurs preview (machine-spécifique)
.claude/launch.json
# Claude — permissions locales machine-spécifiques (convention *.local.json)
.claude/settings.local.json

# OS / IDE
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# NE PAS ignorer les specs et le design :
# docs/ et design/ sont versionnés.
